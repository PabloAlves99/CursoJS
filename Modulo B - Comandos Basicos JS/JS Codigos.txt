parseInt = Numero inteiro 
parsefloat = Numero real
templat strings ${} para escrever variaveis 
s.length quantas letras tem a string
s.toUpperCase() tudo maiusculo
s.toLowerCase() tudo minusculo
<strong></strong>
to.Fixed()
toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'})

operadores:
aritmeticos{ + - * / % (resto da divisão inteira) ** (potencia)}
atribuicao { += -=... o sinal antes do 'igual' significa o calculo do proprio numero, mais, menos ou qualquer coisa }
logico{! negação, && conjunção ('e' logico, os dois verdadeiro),|| disjunção('ou' logico, apenas um verdadeiro)}
relacionais(== igual á... === identico á... != não igual )
ternario (...?'...':...)

idade >= 15 && idade <=17
a idade está entre 15 e 17?

estado == 'RJ' || estado =='SP'
O estado é RJ ou SP?

salário > 1500 && sexo != 'M'
O salário é maior que 1500 e o sexo não é masculino?
DOM - Document object model